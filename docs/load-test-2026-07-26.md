# 교과 평어 전체 생성 부하 테스트

- 실행일: 2026-07-26
- 환경: Vercel Production + Supabase + OpenAI API
- 대상: 실험실 학급
- 학생: 25명
- 과목: 9개(국어, 사회, 도덕, 수학, 과학, 음악, 미술, 체육, 영어)
- 생성 대상: 225건
- AI 배치: 27개(과목별 최대 10명)

## 결과

| 항목 | 결과 |
| --- | ---: |
| 완료 건수 | 225 |
| 실패 건수 | 0 |
| 저장 건수 | 225 |
| 완료 배치 | 27/27 |
| 총 소요 시간 | 372초(6분 12초) |
| 백그라운드 지속 실행 | 정상 |

브라우저 페이지를 열어두지 않은 상태에서 작업을 등록하고 API 상태만 주기적으로 조회함. 작업은 서버에서 끝까지 계속됐으며 학생·과목별 결과가 Supabase에 저장됨.

## 재현 방법

로컬의 무시된 실험실 계정 파일 또는 `LOAD_TEST_EMAIL`, `LOAD_TEST_PASSWORD` 환경 변수를 사용함. 자격증명은 출력하지 않음.

```bash
pnpm test:load-comments start
pnpm test:load-comments status
```

`preflight`는 AI를 호출하지 않고 활성 학생 25명, 모든 과목의 상·중·하 입력 완전성, 예상 학생-과목 결과 수, 최소 배치 호출 수와 잔여 월 한도를 확인함. `start`는 같은 검증을 통과한 뒤 실제 작업을 등록하며, `status`는 최근 작업의 완료·실패·배치·저장 건수와 소요 시간을 출력함.
